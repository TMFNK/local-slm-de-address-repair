# Historical SFT v1 — superseded

This UUID-only training run is retained for provenance. Its split design
does not support a clean held-out benchmark claim; the shipped model is the
clean-gold v3 SFT run.
