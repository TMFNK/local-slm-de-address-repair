# Superseded frozen-evaluation alias

This directory is a historical copy of the definitive clean-gold v3
evaluation. It is retained for provenance only and is not a separate run.

Use `evals/frozen-test-v3/` for the shipped rules, base, and SFT results.
Use `evals/frozen-test-v5p1/` for the rejected targeted SFT experiment.
