# Historical SFT v2 — superseded

This first pair-grouped run is retained for provenance. Its allocator put
only four distinct addresses across the splits, so it is not a usable
generalization benchmark.
